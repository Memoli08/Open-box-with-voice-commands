# Open-box-with-voice-commands
“This project uses Arduino and Python to recognize a password via voice commands. It checks if the password is correct, and if it is incorrect too many times, it blocks the password and sends an email notification.”
